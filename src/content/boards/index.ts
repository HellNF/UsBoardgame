import type { BoardLayout } from "@/engine/types";

/**
 * Disposizioni predefinite del tabellone.
 * TODO(F1-01): creare `classic.ts` (100 celle, 7 scale, 6 serpenti, geometrie della reference)
 * e un validatore che controlla quantità e vincoli (vedi docs/rules.md § Tabellone).
 * TODO(F7-02): generatore casuale da seed.
 */
export const boards: BoardLayout[] = [];
