/**
 * Crea la stanza della coppia (codice, password, nomi dei due posti).
 * Uso previsto: `pnpm room:create -- --code ABCD --name1 Nicolò --name2 ...` (la password viene chiesta a terminale).
 * TODO(F0-03): implementare con il client admin (src/lib/supabase/admin.ts) e hashPassword.
 */
console.error("Non implementato: vedi docs/roadmap.md, task F0-03.");
process.exit(1);
